pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh 'whoami'
        sh './mvnw clean package -Dcheckstyle.skip'
      }
    }
    stage('Generate SBOM') {
      steps {
        sh 'syft packages dir:. --scope AllLayers'
      }
    }
  }
}
